- name: loops
  hosts: local hosts
  tasks:
    - name: looping throught list of items
      ansible.builtin.debug:
      msg: "item :: {{item}}"
      loop: 
        -linux
        -shell
        -ansible